/**
 * 
 *   Represents a distributd system, which uses a Zookeeper Server or cluster
 *   do build a reliable and fault tolerant environment with a distributed 
 *   Configuration.
 *
 **/
package system;

/**
 *
 * @author root
 */
public class SystemState {
    
    /**
     *   For the initial setup ... use this fixed IPs.
     */
    public  String[] zkServerIPs = { "192.168.3.149" };
    
    
    
    
}
